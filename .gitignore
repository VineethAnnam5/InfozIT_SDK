# Gitignore for push-notification-sdk

node_modules/
dist/          # It's common to ignore 'dist' if you only deploy from CI/CD,
               # but for manual npm publish, it's fine for it to be generated locally.
               # 'files' in package.json controls what gets published.
.env
.DS_Store
npm-debug.log*
yarn-debug.log*